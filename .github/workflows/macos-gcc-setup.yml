name: macos-gcc-setup
on: [push, pull_request]
jobs:
  linux:
    name: macos-gcc-setup
    runs-on: macos-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v2
      - name: macos ios armv8
        run: |
          echo "*** cat shell"
          cat /etc/shells
          brew install gcc@9
          gcc-9 -v   
          echo " " >> ~/.zshrc
          echo "alias gcc='gcc-9'" >> ~/.zshrc
          echo "alias cc='gcc-9'" >> ~/.zshrc
          echo "alias g++='g++-9'" >> ~/.zshrc
          echo "alias c++='c++-9'" >> ~/.zshrc
          source ~/.zshrc
          which gcc-9
          echo "****done"
          gcc -v
